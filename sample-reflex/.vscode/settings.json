{
    "python.defaultInterpreterPath": ".venv/bin/python", // pythonの実行環境（プロジェクトごとに書換）
    "python.testing.pytestEnabled": true, // python pytestの使用（プロジェクトごとに書換）
    "python.terminal.activateEnvironment": true, // terminal起動時に仮想環境を利用するか（プロジェクトごとに置換）
}